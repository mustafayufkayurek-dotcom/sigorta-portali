# Smart Takeoff — Testler (S1)

- `calculation.math.spec.ts` — saf matematik
- `s1-vertical-slice.spec.ts` — uçtan uca dikey dilim

Çalıştırma (backend kökünden):

```bash
pnpm exec jest modules/smart-takeoff --config package.json
```
