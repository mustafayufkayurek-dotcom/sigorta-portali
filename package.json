{
  "name": "sigorta-hasar-sistemi",
  "version": "1.0.0",
  "private": true,
  "description": "Sigorta hasar onarım yönetim sistemi",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "smoke:route-gate": "bash scripts/smoke-route-gate.sh",
    "smoke:field-open-list": "bash scripts/smoke-field-open-list.sh",
    "clean": "turbo run clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "preview:docs": "node scripts/serve-preview.js"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "prettier": "^3.2.4",
    "turbo": "^1.12.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.1"
}
